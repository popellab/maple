# PARAMETER DEFINITION (from model context)
# ============================================================================
parameter_name: "k_CD8_pro"
parameter_units: "1/day"
parameter_definition: |
  T cell proliferation rate in tumor microenvironment in presence of growth signals.
  Quantifies CD8+ T cell expansion rate in tumor tissue.

cancer_type: "PDAC"  # Target cancer type or indication

tags: ["quick-estimate", "initial-scoping"]

# Model context snapshot
# ============================================================================
model_context:
  derived_from_context:
    - name: k_CD8_pro
      description: 'CD8 proliferation rate'
  reactions_and_rules:
    - reaction: "null -> V_T.CD8"
      reaction_rate: "k_CD8_pro * V_T.CD8"

context_hash: "a1b2c3d4"  # Hash of model context for tracking

mathematical_role: |
  Describe the mathematical role of this parameter in the model equations.
  Examples:
  - "Rate constant in mass action kinetics"
  - "Michaelis constant in Hill function"
  - "Scaling factor for unit conversion"

# QUICK ESTIMATE DATA
# ============================================================================
parameter_estimate:
  # The ballpark numerical value for this parameter
  value: NUMERIC_VALUE

  # Plausible range for this parameter [min, max]
  range: [MIN_VALUE, MAX_VALUE]

  # Units for the parameter value
  units: "UNITS_STRING"

  # Your confidence in this estimate (High/Medium/Low)
  confidence_level: "CONFIDENCE_LEVEL"

  # Brief explanation of the estimate and key assumptions
  justification: |
    Brief explanation of how this estimate was derived, what source(s) were used,
    and any key assumptions or limitations. Keep this concise but informative.

# The primary source for this estimate
source:
  citation: "FULL_CITATION"
  doi_or_url: "DOI_OR_URL_OR_NA"
  location: "Figure/Table/Page where value was found"
  text_snippet: "Exact text or data point that supports this estimate"

# Important caveats or limitations of this estimate
limitations: |
  - Key limitation 1 (e.g., species difference, measurement method, etc.)
  - Key limitation 2 (e.g., limited sample size, indirect measurement, etc.)
  - Any other important caveats that affect the reliability of this estimate