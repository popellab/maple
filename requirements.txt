# Install the maple package in editable mode
# This automatically installs all dependencies listed in pyproject.toml
-e .

# For development, install optional dev dependencies:
# pip install -e ".[dev]"
